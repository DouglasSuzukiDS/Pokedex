import { PikachuConfused } from "../../assets/Icons/PikachuConfused"
import { ComponentsTypes } from "../../types/ComponentsType"

export const NotFound = ({ phase }: ComponentsTypes) => {
   return(
      <PikachuConfused w="183" h="242" />
   )
}